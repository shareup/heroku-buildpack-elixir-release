#!/usr/bin/env bash

set -e

export IS_TEST=true

BP_DIR=$(cd "$(dirname ${0:-})"; cd ..; pwd)

"$BP_DIR/bin/compile" "$1" "$2" "$3"

mkdir -p "$1/.profile.d"
cp -ar "$BP_DIR/ci-profile/." "$1/.profile.d/"

cd "$1"

mix do deps.get, compile
